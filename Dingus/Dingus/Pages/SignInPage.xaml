<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Dingus"
             x:Class="Dingus.Pages.SignInPage" Title="Dingus">
    <Frame>
        <StackLayout>
            <Label Text="Incorrect login or password" TextColor="Red" HorizontalTextAlignment="Center" IsVisible="{Binding IsUserNotFound}" />
            <Entry Text="{Binding SignInUser.Login, Mode=TwoWay}" Placeholder="Login" IsSpellCheckEnabled="False" />
            <Entry Text="{Binding SignInUser.Password, Mode=TwoWay}" Placeholder="Password" IsPassword="True" />
            <Button Command="{Binding SignInCommand}" Text="Sign In" />
            <Button Command="{Binding SignUpCommand}" Text="Sign Up" />
        </StackLayout>
    </Frame>
</ContentPage>
